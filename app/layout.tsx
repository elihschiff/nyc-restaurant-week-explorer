import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NYC Restaurant Week Explorer · Summer 2026",
  description:
    "Search, filter, map, and compare all 620 restaurants participating in NYC Restaurant Week Summer 2026.",
  icons: {
    icon: `${process.env.PAGES_BASE_PATH ?? ""}/favicon.svg`,
    shortcut: `${process.env.PAGES_BASE_PATH ?? ""}/favicon.svg`,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable}`}
      >
        {children}
      </body>
    </html>
  );
}
